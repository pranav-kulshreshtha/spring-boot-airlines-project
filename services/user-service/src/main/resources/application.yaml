server:
  port: 5001

spring:
  application:
    name: user-service

  datasource:
    url: jdbc:mysql://localhost:3306/msp_airlines_location_db
    username: root
    password: Pkul@4567

  jpa:
    hibernate:
      ddl-auto: update