server:
  port: 5006

spring:
  application:
    name: seat-service

  datasource:
    url: jdbc:mysql://localhost:3306/msp_airlines_seat_db
    username: root
    password: Pkul@4567

  jpa:
    hibernate:
      ddl-auto: update
