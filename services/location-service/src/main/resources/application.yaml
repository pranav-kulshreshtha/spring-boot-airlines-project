server:
  port: 5004

spring:
  application:
    name: location-service

  datasource:
    url: jdbc:mysql://localhost:3306/msp_airlines_location_db
    username: root
    password: Pkul@4567

  jpa:
    hibernate:
      ddl-auto: update